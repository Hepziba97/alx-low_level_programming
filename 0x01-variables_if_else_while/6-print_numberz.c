#include <stdio.h>

/**
 * main - main function
 * Description: print all single numbers below 10
 * Return: 0 (Success
 */
int main(void)
{
	int num;

	for (num = 0; num <= 10; num++)
		putchar(num);
	putchar('\n');
	return (0);
}
